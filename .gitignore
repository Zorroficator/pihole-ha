.env
*_pass
*.key
*.pem
.DS_Store
handoffs/
__pycache__/
*.pyc

# Rotierende Config-Backups (dateiname.bak-s1..s5) — nie versionieren
*.bak-s*

# Live monitoring data — never commit: would leak the household DNS query log,
# client IPs, MAC addresses and device names into a public repo.
data/
dashboard/data.json
dashboard/history.json

# Env files — keep only the committed template
.env*
!.env.example

# Deploy config + render.sh outputs (contain real network addresses / secrets)
config.env
/docker-compose.yml
/keepalived/dns-ha.keepalived.conf
/keepalived/pi-zero.keepalived.conf
/keepalived/notify_telegram.sh
/pi_zero/dnsdist.conf
/pi_zero/monitor_config.toml
/pi_zero/pihole_monitor.py
/pi_zero/pihole-vip-bind.sh
/pi_zero/pihole-vip.service
/server/dashboard_collector.py
/server/pihole_heartbeat.sh
