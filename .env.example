# Template for .env — copy to .env and fill in a real password.
# .env itself is in .gitignore and is never committed.
# Generate a strong value with: openssl rand -base64 24
PIHOLE_API_PASSWORD=changeme
